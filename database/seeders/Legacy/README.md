# Legacy Seeders

This folder contains older seeding utilities retained for reference only.

Active seed path:

- `DatabaseSeeder`
- `CompanySeeder`
- `FullApplicationDemoSeeder`

Only use the legacy seeders here when doing focused historical comparisons or one-off migration work.
