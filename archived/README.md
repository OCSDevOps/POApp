# Archived Sources

This folder contains large historical source archives retained only for reference.

Current application work should happen in the Laravel codebase at the repository root.

Do not rely on files under `archived/` as the current implementation source of truth.
